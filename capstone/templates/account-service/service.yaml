apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.accountService.name }}
spec:
  selector:
    app: {{ .Values.accountService.name }}
  ports:
    - port: 81
      targetPort: {{ .Values.accountService.port }}
  type: {{ .Values.accountService.serviceType }}
